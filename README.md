# Backend Developer Portfolio

실무 경험과 Sadari 기술 사례를 한 페이지에서 이어서 보여주는 정적 포트폴리오다.

## 로컬 실행

```bash
npm install
npm run dev
```

## 검증

```bash
npm run check
npm run build
```

## 배포

Cloudflare Pages에서 저장소를 연결한 뒤 빌드 명령은 `npm run build`, 출력 폴더는 `dist`로 설정한다.
